# フロントエンドの設定はアプリ内「設定」画面から行えます（バックエンドURL等）。
# 以下はバックエンド（Supabase Edge Functions）で使用する環境変数です。

# Claude APIキー（Edge Function 側のシークレットとして設定）
# 例: supabase secrets set ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_API_KEY=
